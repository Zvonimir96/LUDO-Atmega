#pragma once

/**
 * @brief Types of LED animations
 */
enum class AnimationType
{
    GLOBAL_FADE = 0,  ///< Fade in/out effect (for buttons, dice)
    MOVE = 1          ///< Figure movement animation
};